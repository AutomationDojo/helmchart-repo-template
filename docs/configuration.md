# Configuration

## Values

See the chart's `values.yaml` for all available configuration options.

The `README.md` inside the chart directory is auto-generated by `helm-docs` on every PR and release.
